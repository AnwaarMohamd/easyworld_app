abstract final class AppConstants {
  AppConstants._();

  static const String appName = 'Rick & Morty Explorer';

  static const Duration searchDelay =
      Duration(milliseconds: 500);

  static const int pageSize = 20;
}